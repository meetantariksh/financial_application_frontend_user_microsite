// import { fromJS } from 'immutable';
// import { selectSeccondfactorAuthenticationDomain } from '../selectors';

describe('selectSeccondfactorAuthenticationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
